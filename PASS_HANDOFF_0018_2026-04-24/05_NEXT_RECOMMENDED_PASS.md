# Next Recommended Pass

## Recommended next step

Start the first evidence-backed archive/history reintroduction pass.

1. Promote or create the first real `archive_item` record set with canonical source URLs and actual provenance.
2. Keep review-seed records unpublished and hidden.
3. Re-verify `/archive/`, `/history/`, and any relevant direct `history_topic` route after those real records are public.
4. Confirm that direct term routes only return `200` when real public records actually exist behind them.

## Why this is next

The public cleanup slice is now functionally complete for the confirmed residual route leak. The remaining gap is real archive/history content depth, not public exposure of review-era taxonomy residue.
