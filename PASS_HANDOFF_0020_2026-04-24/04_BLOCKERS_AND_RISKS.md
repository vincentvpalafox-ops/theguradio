# Blockers And Risks

## Current blockers

- No blocker prevented completion of this pass

## Remaining risks

- Archive depth is improved but still relatively thin
  - public `archive_item` count is now `5`
  - public history-record count is now `2`
- History-topic coverage is still narrow
  - `timeline` now has two real records
  - `venue-legacy` still returns `404`
- Record naming quality is bounded by the existing media-library evidence
  - `Livestream PNG` is a real source-backed title, but it is less polished than a curator-written public title would be
