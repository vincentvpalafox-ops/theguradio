# Next Recommended Pass

## Recommended next step

Run the third evidence-backed archive reintroduction slice, with a bias toward better history-topic coverage.

1. Add the next `2` to `3` real archive records from existing hosted media or another canonical source already under GU control.
2. Prioritize at least one record that can defensibly support a history topic other than `timeline`.
3. If no evidence-backed `venue-legacy` candidate exists, leave that route dark and add only flyer, poster, or media records with stronger provenance.
4. Keep review-seed records unpublished and hidden.
5. Re-verify `/archive/`, `/history/`, and only the direct term routes that now have real public records.

## Why this is next

The archive is no longer empty and now has basic type coverage across `media`, `flyer`, `poster`, and `history`. The next quality gap is broader history-topic coverage and deeper public archive density.
