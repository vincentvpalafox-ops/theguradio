# Blockers And Risks

## Blockers

- No blocker prevented completion of this pass.

## Remaining risks

- Public archive browse filtering appears to have a real defect:
  - `/archive/?year=2023` returns `404`
  - this likely reflects a collision with the reserved `year` query var

- No evidence-backed non-`timeline` history candidate was recoverable from this live media slice.
  - that is not a code blocker
  - it does mean history-topic breadth still remains intentionally narrow

- `venue-legacy` remains dark because there is still no public evidence-backed record supporting that term.
