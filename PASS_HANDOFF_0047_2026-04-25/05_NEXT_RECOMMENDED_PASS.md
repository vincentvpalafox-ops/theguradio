# Next Recommended Pass

Keep the archive-backed homepage active.

If there is a follow-up pass, it should be limited to post-activation observation only:

1. watch live behavior on `/`
2. confirm no unexpected cache regressions appear
3. decide later whether the old preview slug needs any editorial cleanup beyond its current redirect to `/`

Do not widen that follow-up into redesign, provider work, search changes, or unrelated cleanup.
